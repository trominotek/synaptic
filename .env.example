# Synaptic Application Configuration

# Application Version (will be set by build script)
VERSION=latest

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=ai_application
DB_USER=postgres
DB_PASSWORD=postgres
DB_SCHEMA=public

# Service Ports (internal container ports)
MCP_SERVER_PORT=8000
AGENTS_PORT=8001
OCR_PORT=5000

# Environment
ENVIRONMENT=production
FLASK_ENV=production
NODE_ENV=production

# Logging
LOG_LEVEL=INFO